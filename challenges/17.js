// Escribe un algoritmo que al recibir el texto de un usuario, devuelva el
// número de palabras y el número de caracters en ese texto.

const { read, readNumber, print } = require('../helpers')

const main = async () => {
  print('Problema aún no resuelto')
}

module.exports = main
