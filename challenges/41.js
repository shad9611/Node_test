// Escriba un algoritmo que tome un array de temperaturas en grados Celsius y
// devuelva otro array con una lista equivalente en grados Farenheit

const { read, readNumber, print } = require('../helpers')

const celsiusTemperatures = [10, 25, 5, 32, 15, 20, 0, -5]

const main = async () => {
  print('Problema aún no resuelto')
}

module.exports = main
